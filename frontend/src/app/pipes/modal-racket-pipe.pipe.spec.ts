import { ModalRacketPipePipe } from './modal-racket-pipe.pipe';

describe('ModalRacketPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ModalRacketPipePipe();
    expect(pipe).toBeTruthy();
  });
});
